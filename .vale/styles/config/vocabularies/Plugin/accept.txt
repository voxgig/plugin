# Terms Vale's dictionary does not know. Entries are REGEXES, so
# [Vv]oxgig accepts both casings without Vale.Terms then enforcing one of
# them over the other -- listing both plain forms is what turns this file
# into a capitalisation rule by accident.
#
# EVERY ENTRY BELOW WAS MEASURED, not guessed. `Vale.Spelling` at error
# level over the 19 reader-facing pages produced 182 alerts across 72
# distinct words on a clean run; this file is those words, sorted into
# the groups below, and nothing else. A word that stops appearing in the
# docs should come out of here, and a new one is added when the gate asks
# for it.

[Vv]oxgig

# British spellings. Google style is US English; the house voice is not,
# and the precedence in .vale.ini puts the house voice first.
#
# LISTED, not matched by suffix. `\w+ise` and friends accept any word
# ending in those letters -- `madeupise` included -- and so punch a hole
# straight through the error-level spelling gate this file exists to make
# usable. Add the stem when a new one is needed; the cost of doing that is
# the point.
labour

# The ports, named the way their directories are: lowercase, as the
# pages write them everywhere. A port is `php/`, and the prose says php.
clojure
csharp
javascript(?:'s)?
kotlin
[Ll]ua
perl(?:'s)?
php(?:'s)?
scala(?:'s)?

# The plugin model, the corpus, and the neighbouring Voxgig projects.
aontu
api
APIs
canonical's
Canonicalizing
config
noisy's
omni
sdkgen(?:'s)?
sekreto
SDKs?(?:'s)?
teardowns?
uncanonicalized
undef
undoable

# General programming vocabulary the port pages use.
async
autovivifies
[Bb]oolean
coderef
coroutine
dicts
enum
falsy
func
hashrefs
introsort
lookups
lossy
metacharacter
microtask
mtime
multiline
namespace
quicksort
repo(?:'s)?
scalar's
stderr
stringify
toolchain
truthy
unescape[ds]
untyped

# Toolchains and test frameworks the port pages say they do without.
gradle
JDK's
JUnit
leiningen
luarocks
munit
OSGi
PHPUnit
pubspec
sbt
xunit
